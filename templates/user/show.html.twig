{% extends 'back.html.twig' %}

{% block title %}User
{% endblock %}

{% block body %}
<div>
						Admin:
						{{ admin.nom }}
						|
						<a href="{{ path('app_logout') }}">Déconnexion</a>
					</div>

	<h1>User</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>image</th>
				<td>
					<center><img src="{{ asset('/uploads/user/' ~ user.getImage()) }}" width="300px" height="200px" alt=""/></td>
				</td>
			</tr>
		</tbody>
	</center>
</tr>
<tr>
	<th>Id</th>
	<td>{{ user.id }}</td>
</tr>
<tr>
	<th>Nom</th>
	<td>{{ user.nom }}</td>
</tr>
<tr>
	<th>Prenom</th>
	<td>{{ user.prenom }}</td>
</tr>
<tr>
	<th>Email</th>
	<td>{{ user.email }}</td>
</tr>
<tr>
	<th>Mdp</th>
	<td>{{ user.mdp }}</td>
</tr>
<tr>
	<th>Role</th>
	<td>{{ user.role }}</td>
</tr>
<tr>
	<th>Date_birth</th>
	<td>{{ user.dateBirth ? user.dateBirth|date('Y-m-d') : '' }}</td>
</tr>

<tr>
	<th>Access</th>
	{% if user.isBlocked %}
		<td>Blocked</td>
	{% else %}
		<td>Autorisé</td>
	{% endif %}
</tr></tbody></table><a href="{{ path('app_user_index') }}">back to list</a><a href="{{ path('app_user_edit', {'id': user.id}) }}">edit</a>{{ include('user/_delete_form.html.twig') }}</center>{% endblock %}
