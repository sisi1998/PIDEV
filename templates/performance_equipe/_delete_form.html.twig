<form method="post" action="{{ path('app_performance_equipe_delete', {'id': performance_equipe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ performance_equipe.id) }}">
    <button class="btn">Delete</button>
</form>
